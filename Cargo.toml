[package]
name = "fhir-generate"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
easy-tree = { path = "../easy-tree" }
fmt-derive = "0.1.2"
serde_json = "1.0.149"

[dev-dependencies]
assert_cmd = "2.2.0"
